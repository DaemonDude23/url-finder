builds:
  - binary: url-finder
    dir: ./src
    env:
      - CGO_ENABLED=0
    goarch:
      - amd64
      - arm64
      - '386'
    goos:
      - linux
      - darwin
      - windows
    id: url-finder
checksum:
  name_template: checksums.txt
release:
  draft: true
